<%- include('./partials/header') %>
<% if (error && error.length > 0) { %>  <!-- Ensure error exists and has length -->
    <div class="absolute top-5 left-1/2 -translate-x-1/2 -translate-y-1/2 p-3 rounded-md bg-red-500 font-[Poppins]">
        <span class="inline-block mt-1 mb-1 text-white">
            <%= error[0] %> <!-- Show the first error message -->
        </span>
    </div>
<% } %>
<div class="bg-gray-100 min-h-screen font-[Poppins] flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <div class="w-full px-32 py-5">

            <h3 class="text-3xl font-[Poppins]">Login to <span class="text-blue-400 font-semibold font-sans">Scatch</span></h3>
        </div>
        <form action="/users/login" method="post">
            <div class="mb-4">
                <label for="email" class="block text-gray-700 font-semibold mb-2">Email</label>
                <input type="email" id="email" name="email" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-gray-400" required>
            </div>
            <div class="mb-6">
                <label for="password" class="block text-gray-700 font-semibold mb-2">Password</label>
                <input type="password" id="password" name="password" class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-gray-400" required>
            </div>
            <button type="submit"  class="w-full bg-blue-400 text-white py-3 rounded-md font-bold transition duration-300">Login</button>
        </form>
        <div class="mt-4 text-center">
            <a href="/forgot-password" class="text-gray-600 hover:text-gray-800">Forgot password?</a>
        </div>
        <div class="mt-6 text-center">
            <p>Don't have an account? <a href="/signup" class="text-blue-500 hover:text-blue-600">Sign up</a></p>
        </div>
    </div>
</div>

<%- include('./partials/footer') %>